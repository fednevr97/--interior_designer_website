/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  // Удалите или закомментируйте experimental.serverActions если не используете
  // experimental: {
  //   serverActions: true,
  // },
};

module.exports = nextConfig;
